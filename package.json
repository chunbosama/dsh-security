{
  "name": "dsh-security",
  "description": "A pluggable security suite for DeepSeek Harness (dsh): everything-is-a-plugin modules for attack alerts, command-execution protection, critical-file protection, strange-IP protection, and /api protection, managed from a Settings control panel.",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r --filter './packages/**' run bundle"
  },
  "license": "MIT"
}
